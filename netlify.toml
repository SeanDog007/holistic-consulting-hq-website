[build]
  command = "export DATABASE_URL=\"${DATABASE_URL:-$NETLIFY_DB_URL}\" && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev"

[build.processing]
  skip_processing = true
